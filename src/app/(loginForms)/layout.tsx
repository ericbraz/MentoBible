import FormPagesLayout from '@/components/FormPagesLayout'

export default function AuthLayout({ children }: { children: React.ReactNode }) {
   return <FormPagesLayout>{children}</FormPagesLayout>
}
